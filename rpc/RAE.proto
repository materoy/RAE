syntax = "proto3";

option go_package = "github.com/rmgen/RAE";

package protobuf;

service RAEStreamService {
    rpc FetchResponse (Request) returns (stream Response);
}

message Request {
    int32 id = 1;
}

message Response {
    string result = 1;
}